Create a function which will accept salary array as input and finds gratest salary among all salaries and return that salary

Create a function which will accept name array as input and finds all names which are statring with letter 'P'/'p' and returns 
new array which contains result

Create a function which will accept name array as input and finds first name which are statring with letter 'P'/'p' and returns
that result

Create a function which will accept age array as input. Each age from array should be doubled and saved in new array.
Return new array as a result


